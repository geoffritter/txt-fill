
/**
 * Will wrap each paragraph in nothing, one or more br elements, new lines (n), spaces (s), or any string as an html element.
 * @typedef { "false" | "br" | "n" | "\n" | "s" | " " | string } WRAP
 */

export default {};